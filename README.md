
  # Generate HTML and JSON

  This is a code bundle for Generate HTML and JSON. The original project is available at https://www.figma.com/design/S39PtGPZfT54KIb0qHw82Y/Generate-HTML-and-JSON.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  